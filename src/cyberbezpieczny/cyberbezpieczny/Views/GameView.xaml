<UserControl x:Class="cyberbezpieczny.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Style x:Key="CyberListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#A0A0A0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3300F2FF"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#00F2FF"/>
                                <Setter TargetName="bd" Property="Background" Value="#1000F2FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="#8000F2FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" BorderThickness="2" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{Binding BorderBrush.Color, RelativeSource={RelativeSource TemplatedParent}}" BlurRadius="15" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#1a1a2e">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="TERMINAL ANALIZY WIADOMOŚCI" Foreground="#00F2FF" FontSize="24" FontWeight="Bold">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#00F2FF" BlurRadius="10" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="STATUS SYSTEMU: " Foreground="#A0A0A0" FontFamily="Consolas"/>
                <TextBlock Text="AKTYWNY | WYNIK: 0000" Foreground="#00F2FF" FontFamily="Consolas" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#3300F2FF" BorderThickness="1" CornerRadius="10" Background="#10000000" Padding="10">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="&lt; SKRZYNKA ODBIORCZA &gt;" Foreground="#A0A0A0" FontFamily="Consolas" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <ListBox ItemsSource="{Binding Emails}" 
                             SelectedItem="{Binding CurrentEmail}"
                             Background="Transparent" 
                             BorderThickness="0" 
                             ItemContainerStyle="{StaticResource CyberListBoxItem}">

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Subject}" 
                                               FontWeight="Bold" 
                                               FontSize="13"
                                               TextTrimming="CharacterEllipsis"/>

                                    <TextBlock Text="{Binding Sender}" 
                                               FontSize="11" 
                                               Foreground="#80FFFFFF"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="#00F2FF" BorderThickness="1" CornerRadius="10" Background="#0800F2FF" Padding="20" Margin="0,0,0,20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>

                            <Border BorderBrush="#3300F2FF" BorderThickness="0,0,0,1" Padding="0,0,0,10" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="{Binding CurrentEmail.Sender, StringFormat='OD: {0}'}" 
                                               Foreground="#A0A0A0" 
                                               FontFamily="Consolas"
                                               TextWrapping="Wrap"/>

                                    <TextBlock Text="{Binding CurrentEmail.Subject, StringFormat='TEMAT: {0}'}" 
                                               Foreground="#00F2FF" 
                                               FontSize="18" 
                                               FontWeight="Bold" 
                                               Margin="0,5,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="{Binding CurrentEmail.Content}" 
                                       Foreground="White" 
                                       FontFamily="Consolas" 
                                       TextWrapping="Wrap" 
                                       FontSize="14"/>

                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">

                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Background="#d32f2f" BorderBrush="#ff4444" 
                            Width="250" Height="50" Margin="10"
                            Command="{Binding MarkAsPhishingCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💀 "/>
                            <TextBlock Text="ZGŁOŚ PHISHING"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource ActionButtonStyle}" 
                            Background="#2e7d32" BorderBrush="#00e676" 
                            Width="250" Height="50" Margin="10"
                            Command="{Binding MarkAsSafeCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✅ "/>
                            <TextBlock Text="OZNACZ JAKO BEZPIECZNE"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </Grid>
        </Grid>
    </Grid>
</UserControl>