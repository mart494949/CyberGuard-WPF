<UserControl x:Class="cyberbezpieczny.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="CyberButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderThickness="2" BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00F2FF"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00F2FF" BlurRadius="10" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>

        <Grid Background="#1a1a2e">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="CYBER-OCHRONA" Foreground="#00F2FF" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00F2FF" BlurRadius="15"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="SYSTEM SZKOLENIOWY v.1.0" Foreground="#A0A0A0" FontSize="12" FontFamily="Consolas" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <Button Style="{StaticResource CyberButtonStyle}" Background="#2980b9" Height="60" Width="300" Margin="0,10" Command="{Binding VerifyCommand}">
                    <TextBlock Text="🔍  STACJA WERYFIKACJI"/>
                </Button>
                <Button Style="{StaticResource CyberButtonStyle}" Background="#d35400" Height="60" Width="300" Margin="0,10" Command="{Binding FirewallCommand}">
                    <TextBlock Text="🛡️  STRAŻNIK ZAPORY"/>
                </Button>
                <Button Style="{StaticResource CyberButtonStyle}" Background="Transparent" BorderBrush="#ff4444" Foreground="#ff4444" Height="40" Width="150" Margin="0,30,0,0" Command="{Binding ExitCommand}">
                    <TextBlock Text="❌ ZAKOŃCZ SESJĘ"/>
                </Button>
            </StackPanel>
        </Grid>


        <Grid Background="#1a1a2e" Visibility="{Binding IsIntroVisible, Converter={StaticResource BoolToVis}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40" MaxWidth="700">

                    <TextBlock Text="ODPRAWA SZKOLENIOWA" Foreground="#00F2FF" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="STATUS: INICJALIZACJA..." Foreground="#A0A0A0" FontFamily="Consolas" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <Border Background="#20FFFFFF" CornerRadius="10" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="⚠️ CEL MISJI: OCHRONA DANYCH" Foreground="#ffcc00" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                            <TextBlock Text="Witaj w symulatorze. Twoim zadaniem jest ochrona infrastruktury przed cyberatakami. Zapoznaj się z narzędziami:" Foreground="White" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#102980b9" BorderBrush="#2980b9" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="🎭" FontSize="30" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="White"/>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="MODUŁ 1: WYKRYWANIE OSZUSTW" Foreground="#5dade2" FontWeight="Bold"/>

                                <TextBlock Text="Zagrożenie: Hakowanie Ludzi (Scam)." Foreground="#a9cce3" FontStyle="Italic" Margin="0,2,0,2" FontSize="11"/>

                                <TextBlock Text="Nauka: Hakerzy nie zawsze łamią hasła siłą. Częściej atakują Twój umysł, udając kogoś zaufanego (np. admina gry lub bank). Tutaj nauczysz się rozpoznawać fałszywe wiadomości i nie dać się nabrać." 
                                           Foreground="#eaf2f8" FontSize="12" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Background="#10d35400" BorderBrush="#d35400" BorderThickness="1" CornerRadius="10" Padding="15" Margin="0,0,0,30">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🛡️" FontSize="30" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="STRAŻNIK ZAPORY (Firewall)" Foreground="#e59866" FontWeight="Bold"/>
                                <TextBlock Text="Nauka: Filtracja pakietów sieciowych i blokowanie złośliwego oprogramowania (Malware)." Foreground="#fbeee6" FontSize="12" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Button Height="50" Width="250" Background="Transparent" BorderBrush="#00F2FF" BorderThickness="2" Foreground="#00F2FF"
                            Cursor="Hand" Command="{Binding CloseIntroCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="b" CornerRadius="25" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="2">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="b" Property="Background" Value="#2000F2FF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock Text="ROZUMIEM - PRZEJDŹ DO SYSTEMU" FontWeight="Bold"/>
                    </Button>

                </StackPanel>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>